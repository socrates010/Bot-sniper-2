flask==3.0.3
ccxt>=4.4.0
# pandas travado em 2.x: pandas_ta ainda não é testado/estável contra o
# pandas 3.x (lançado recentemente), que quebraria vários indicadores.
pandas>=2.2.0,<3.0.0
# A versão antiga (0.3.14b0) foi removida do PyPI em set/2025. As versões
# atuais (0.4.x) exigem Python >= 3.12 — por isso fixamos Python 3.12 no
# arquivo .python-version deste repositório.
pandas_ta>=0.4.67b0
requests>=2.31.0
 
# coincurve foi removido de propósito: é um pacote OPCIONAL (só acelera a
# assinatura ECDSA da Hyperliquid) que costuma falhar ao compilar em versões
# recentes do Python (ex: 3.14) por um problema de empacotamento do cffi/
# hatchling, não do seu código. Sem ele o ccxt usa um fallback puro em
# Python para assinar as chamadas — funciona normalmente, só um pouco mais
# lento (imperceptível no ciclo do bot). Se quiser reativar no futuro, use
# uma imagem com Python 3.11/3.12 e teste local antes de subir pro Render.
 
